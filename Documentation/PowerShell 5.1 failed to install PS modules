Issue:   PowerShell 5.1 failed to install PS modules 
Cause: PowerShell 5.1 defaults only enabling SSL3 and TLS 1.0 for secure HTTP connections and PowerShellGallery disables support for TLS 1.0 
In order to enable Tls12 as the SecurityProtocolfor for Powershell is create a profile loads(profile.ps1)
	• Check PowerShell version
		C:\Windows\system32> $PSVersionTable
	• Verify TLS version on server
		C:\Windows\system32> [Net.ServicePointManager]::SecurityProtocol
	• Create a profile.ps1 C:\Windows\System32\WindowsPowerShell\v1.0\Profile.ps1
	$ProfileFile = "${PsHome}\Profile.ps1"
	if (! (Test-Path $ProfileFile)) {
		New-Item -Path $ProfileFile -Type file -Force
	}
	'[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12' | Out-File -FilePath $ProfileFile -Encoding ascii -Append
	
Reference link : https://rnelson0.com/2018/05/17/powershell-in-a-post-tls1-1-world/
*** PowerShell 7

Get-InstalledModule
